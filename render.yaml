services:
  - type: web
    name: dna-sequence-matcher
    runtime: docker
    plan: free
    branch: main
    dockerfilePath: ./Dockerfile
    autoDeploy: true
    envVars:
      - key: SPRING_JPA_HIBERNATE_DDL_AUTO
        value: update
